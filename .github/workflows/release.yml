name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  version-guard:
    name: version-guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Assert plugin.json version matches the tag
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME#v}"
          pj="$(jq -r .version .claude-plugin/plugin.json)"
          echo "tag=$tag  plugin.json=$pj"
          if [ "$tag" != "$pj" ]; then
            echo "::error::Tag v$tag does not match .claude-plugin/plugin.json version '$pj'. Bump plugin.json (and CHANGELOG) before tagging — Claude Code keys /plugin update on plugin.json version, so a stale value silently blocks the release from reaching users." >&2
            exit 1
          fi

  build:
    name: build (${{ matrix.target }})
    needs: version-guard
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
            cross: true
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross (linux aarch64 only)
        if: matrix.cross == true
        run: cargo install cross --locked

      - name: Build (cargo)
        if: matrix.cross != true
        run: cargo build --release --target ${{ matrix.target }} --bin cortex-mcp --bin cortex-session-start --bin cortex-post-tool-use --bin cortex-session-end --bin cortex-migrate

      - name: Build (cross)
        if: matrix.cross == true
        run: cross build --release --target ${{ matrix.target }} --bin cortex-mcp --bin cortex-session-start --bin cortex-post-tool-use --bin cortex-session-end --bin cortex-migrate

      - name: Stage artifacts (unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        env:
          TARGET: ${{ matrix.target }}
        run: |
          STAGE="claude-cortex-${TARGET}"
          mkdir -p "$STAGE"
          for bin in cortex-mcp cortex-session-start cortex-post-tool-use cortex-session-end cortex-migrate; do
            cp "target/${TARGET}/release/$bin" "$STAGE/"
          done
          cp README.md LICENSE "$STAGE/"
          tar czf "$STAGE.tar.gz" "$STAGE"
          shasum -a 256 "$STAGE.tar.gz" > "$STAGE.tar.gz.sha256"

      - name: Stage artifacts (windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        env:
          TARGET: ${{ matrix.target }}
        run: |
          $stage = "claude-cortex-$Env:TARGET"
          New-Item -ItemType Directory -Path $stage | Out-Null
          foreach ($bin in @("cortex-mcp", "cortex-session-start", "cortex-post-tool-use", "cortex-session-end", "cortex-migrate")) {
            Copy-Item "target/$Env:TARGET/release/$bin.exe" "$stage/"
          }
          Copy-Item README.md $stage
          Copy-Item LICENSE $stage
          Compress-Archive -Path $stage -DestinationPath "$stage.zip"
          $hash = (Get-FileHash "$stage.zip" -Algorithm SHA256).Hash.ToLower()
          "$hash *$stage.zip" | Out-File -FilePath "$stage.zip.sha256" -Encoding ascii

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: claude-cortex-${{ matrix.target }}
          path: |
            claude-cortex-${{ matrix.target }}.*

  release:
    name: release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
          # Auto-mark RC / alpha / beta / pre tags as prerelease so
          # marketplace consumers don't pick them up as "latest".
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-alpha') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-pre') }}
