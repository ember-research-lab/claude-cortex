/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Never commit private keys or runtime flock sentinels
**/.private_key
**/.*.json.lock
.claude/

# Per-session mining drafts (content gets promoted to the ledger; the
# files themselves are scratch and shouldn't track between machines).
.cortex-candidates/
