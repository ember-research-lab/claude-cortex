[package]
publish = false
name = "cortex-confidence"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Durable, decaying per-behavior confidence — the trust signal behind progressive autonomy. A derived view over outcomes; reuses cortex-core's confidence math (deltas + 180-day half-life)."

[dependencies]
cortex-core.workspace = true
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
chrono.workspace = true

[dev-dependencies]
tempfile.workspace = true
