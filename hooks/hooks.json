{
  "description": "claude-cortex Rust hooks: orient on session start, discovery-tag after substantive tool calls, extract outcomes on session end. The hook commands resolve to bin/ shims (auto-added to PATH by Claude Code) until you build the real binaries with `bash \"$CLAUDE_PLUGIN_ROOT/install.sh\"`; until then the shims degrade gracefully (SessionStart prints install instructions, the rest no-op).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cortex-session-start"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cortex-post-tool-use"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cortex-session-end"
          }
        ]
      }
    ]
  }
}
